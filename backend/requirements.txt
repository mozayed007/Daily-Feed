# Daily Feed - News Aggregator Backend v2
# Agent loop architecture with tool system

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data & Database
pydantic>=2.5.0
pydantic-settings>=2.1.0
sqlalchemy>=2.0.23
aiosqlite>=0.19.0
alembic>=1.18.4  # Database migrations

# RSS & Content
feedparser>=6.0.12
beautifulsoup4>=4.12.2
lxml>=4.9.3
trafilatura>=2.0.0  # URL-to-clean-text extraction for article pipeline

# HTTP Client
httpx>=0.25.0  # For async HTTP with timeouts
aiohttp>=3.13.5
requests>=2.33.1

# AI/LLM - Unified via litellm through pydantic-ai
pydantic-ai>=1.93.0
pydantic-graph>=0.0.21
pydantic-ai-litellm>=0.2.6
litellm>=1.59.0
ollama>=0.1.0

# Utilities
pyyaml>=6.0
python-dateutil>=2.8.2
python-jose[cryptography]>=3.3.0
structlog>=25.5.0
tenacity>=8.2.3  # Retry logic
validators>=0.35.0  # URL validation
slowapi>=0.1.9  # Rate limiting

# Voice Assistant (Jarvis / Friday) — Local AI models from Hugging Face
faster-whisper>=1.0.0        # STT engine (Distil-Whisper via CTranslate2)
huggingface-hub>=0.21      # Download Kokoro & Distil-Whisper weights from HF
onnxruntime>=1.17            # Kokoro ONNX inference (local, no cloud)
soundfile>=0.12              # Audio file I/O for Kokoro output
sounddevice>=0.5.5           # Microphone + speaker audio I/O
duckduckgo-search>=8.0.0     # Free web search, no API key
numpy>=2.2.6                # Audio array manipulation
edge-tts>=7.0.0              # Fallback TTS (if Kokoro ONNX fails)

# MCP Server (Agent Experience)
mcp>=1.0.0

# Testing
pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0
httpx>=0.25.0

# Development
black>=23.11.0
isort>=8.0.1
mypy>=1.7.0
passlib[bcrypt]
